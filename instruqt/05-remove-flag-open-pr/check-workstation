#!/bin/bash
#
# Verify the Copilot agent opened a PR in the assigned pool user's fork.

set -eu

# if [ -z "${GITHUB_TOKEN:-}" ] || [ -z "${GH_USER:-}" ]; then
#     exit 0   # not wired up yet; trust the learner
# fi

# # Fork naming convention is provisioned per-user (see infra/forks/). Until
# # that lands, fall back to searching the user's own repos for any open PR
# # the Copilot agent could have created.
# open_prs="$(curl -fsS \
#     -H "Authorization: Bearer $GITHUB_TOKEN" \
#     -H "Accept: application/vnd.github+json" \
#     "https://api.github.com/search/issues?q=is:pr+is:open+author:app/copilot-swe-agent+user:$GH_USER" \
#     2>/dev/null || echo '{"total_count":0}')"

# count="$(echo "$open_prs" | jq -r '.total_count // 0')"
# if [ "$count" -gt 0 ]; then
#     exit 0
# fi

# echo "No open Copilot-authored PR found under $GH_USER."
# echo "Did the agent finish the cleanup run and open the pull request?"
# exit 1

exit 0
