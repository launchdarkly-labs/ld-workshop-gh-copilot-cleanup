#!/bin/bash
#
# No automatable check today. GitHub's REST API exposes only a GET for the
# Copilot cloud-agent config (added 2026-05-18), and even that requires a
# token with Copilot read scope that our GitHub App doesn't carry. Trust
# the learner; downstream challenges fail visibly if the MCP entry is
# missing or malformed.

exit 0
