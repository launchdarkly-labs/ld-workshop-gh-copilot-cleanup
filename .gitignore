# Local-only secrets — sources of truth live in AWS Secrets Manager
docs/github-creds.txt
docs/*.pem

# Python
__pycache__/
*.pyc
.venv/

# Lambda build artifact (recreated by deploy.sh)
infra/sweeper/sweeper.zip

# Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example

# macOS
.DS_Store
test.sh
test.py
test.json
customer-questions.jsonl
